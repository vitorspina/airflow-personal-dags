[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "my_python_test_package_vitor_pina"
version = "0.1.0"
requires-python = ">=3.9"
authors = [{ name = "Vitor Piña" }]
dependencies = []

[tool.setuptools]
package-dir = { "" = "src" }

[tool.setuptools.packages.find]
where = ["src"]